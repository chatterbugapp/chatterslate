language: node
sudo: false
dist: trusty
cache:
  yarn: true
  directories:
    - node_modules
before_install:
  - nvm install 8.8.1
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.3.2
install:
  - yarn install
script:
  - yarn ci
